<%- if controller_name != 'sessions' %>
  <div class="row mt-3">
    <h2 class="bg-light px-3">既に登録済みの方</h2>
  </div>
  <div class="row mt-3">
    <div class="col-1"></div>
    <%= link_to "こちら", new_session_path(resource_name) %>　からログインしてください。
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="row mt-3">
    <h2 class="bg-light px-3">会員登録がお済みでない方</h2>
  </div>
  <div class="row mt-3">
    <div class="col-1"></div>
    <%= link_to "こちら", new_registration_path(resource_name) %>　から新規登録を行なってください。
  </div>
<% end %>

